'use strict';

/**
 * @ngdoc service
 * @name providerApp.interceptorService
 * @description
 * # interceptorService
 * Service in the providerApp.
 */
angular.module('providerApp')
  .service('interceptorService', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
